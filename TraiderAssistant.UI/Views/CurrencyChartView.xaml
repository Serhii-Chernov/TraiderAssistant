<Page x:Class="TraiderAssistant.UI.Views.CurrencyChartView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      xmlns:local="clr-namespace:TraiderAssistant.UI.Views"
      xmlns:viewModels="clr-namespace:TraiderAssistant.UI.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      d:DataContext="{d:DesignInstance viewModels:CurrencyChartViewModel}">

    <Grid>
        <!-- Разметка для отображения данных -->
        <ScrollViewer>


            <Grid Margin="10,10,10,0" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="300"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!--<ToolBar HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"  >
            <ComboBox ItemsSource="{Binding ChartTypes}" SelectedItem="{Binding SelectedChartType}"  Width="70"  />
            <Button Content="1 day" Command="{Binding LoadDayDataCommand}" Padding="5, 0, 5, 0" />
            <Button Content="1 week" Command="{Binding LoadWeekDataCommand}" Padding="5, 0, 5, 0" />
            <Button Content="1 month" Command="{Binding LoadMonthDataCommand}" Padding="5, 0, 5, 0" />
            <Button Content="1 year" Command="{Binding LoadYearDataCommand}" Padding="5, 0, 5, 0" />
        </ToolBar>-->

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5"  >
                    <ComboBox ItemsSource="{Binding ChartTypes}" SelectedItem="{Binding SelectedChartType}"  Width="70"  />
                    <Button Content="1 day" Command="{Binding LoadDayDataCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="1 week" Command="{Binding LoadWeekDataCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="1 month" Command="{Binding LoadMonthDataCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="1 year" Command="{Binding LoadYearDataCommand}" Padding="5, 0, 5, 0" />
                </StackPanel>


                <!-- График -->

                <lvc:CartesianChart Grid.Row="1" Series="{Binding Series}" LegendLocation="Top">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Days" Labels="{Binding Labels}" />
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Price (USD)" LabelFormatter="{Binding YFormatter}"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="5"  >
                    <Button Content="1 m" Command="{Binding LoadOneMinuteTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="5 m" Command="{Binding LoadFiveMinutesTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="15 m" Command="{Binding LoadFifteenMinutesTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="30 m" Command="{Binding LoadThirtyMinutesTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="1 h" Command="{Binding LoadOneHourTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="2 h" Command="{Binding LoadTwoHoursTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="4 h" Command="{Binding LoadFourHoursTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="1 d" Command="{Binding LoadOneDayTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="1 week" Command="{Binding LoadOneWeekTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                    <Button Content="1 month" Command="{Binding LoadOneMonthTechnicalAnalysisCommand}" Padding="5, 0, 5, 0" />
                </StackPanel>

                <!--<ScrollViewer Grid.Row="3" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <local:TechnicalAnalysisView DataContext="{Binding TechnicalAnalysisResult}" />-->

                <StackPanel Grid.Row="3">
                    <StackPanel Orientation="Horizontal"   >
                        <local:TechnicalAnalysisIndicatorView Margin="10,0,0,0" TechnicalAnalysisName="Oscillators" TechnicalAnalysisIndicator="{Binding TechnicalAnalysisResult.OscillatorIndicator}" TechnicalAnalysisText="{Binding TechnicalAnalysisResult.OscillatorResultText}" />
                        <local:TechnicalAnalysisIndicatorView Margin="10,0,10,10" TechnicalAnalysisName="General"  TechnicalAnalysisIndicator="{Binding TechnicalAnalysisResult.GeneralIndicator}" TechnicalAnalysisText="{Binding TechnicalAnalysisResult.GeneralResultText}" />
                        <local:TechnicalAnalysisIndicatorView Margin="0,0,0,0" TechnicalAnalysisName="MAs"  TechnicalAnalysisIndicator="{Binding TechnicalAnalysisResult.MAIndicator}" TechnicalAnalysisText="{Binding TechnicalAnalysisResult.MAResultText}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <local:TechnicalAnalysisTableView Margin="10, 10, 10, 0" TechnicalAnalysisTableName="Oscillators" TechnicalAnalysisTableData="{Binding TechnicalAnalysisResult.Oscillators}" />
                        <local:TechnicalAnalysisTableView Margin="10, 10, 10, 0" TechnicalAnalysisTableName="MovingAverages" TechnicalAnalysisTableData="{Binding TechnicalAnalysisResult.MovingAverages}" />
                    </StackPanel>
                </StackPanel>

            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
